apiVersion: v1
kind: Secret
metadata:
  name: opsmap-secrets
  namespace: opsmap
  labels:
    app.kubernetes.io/name: opsmap
type: Opaque
stringData:
  # IMPORTANT: Replace these with actual secrets in production
  # Use sealed-secrets, external-secrets, or vault for production
  DATABASE_URL: "postgresql://opsmap:CHANGE_ME@postgresql:5432/opsmap"
  REDIS_URL: "redis://redis:6379"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION_USE_STRONG_SECRET"
