apiVersion: v1
kind: ConfigMap
metadata:
  name: opsmap-config
  namespace: opsmap
  labels:
    app.kubernetes.io/name: opsmap
data:
  NODE_ENV: "production"
  PORT: "3000"
  LOG_LEVEL: "info"
  CORS_ORIGIN: "https://opsmap.example.com"
